<%= form_for @parser, html: { multipart: true } do |f| %>
  <% if @parser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parser.errors.count, "error") %> prohibited this parser from being saved:</h2>

      <ul>
      <% @parser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :file %>
    <%= f.file_field :file %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
